import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sjc.studuentforum',
  appName: 'SJC Student Forum',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
