import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sjc.studuentforum',
  appName: 'College Forum',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
